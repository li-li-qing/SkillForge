# ArcheRage 加载后文本行为评测

- 日期：2026-09-12。
- 条件：forward，加载 skills/skillforge-archerage-addon；独立子代理上下文，继承父任务模型设置，未请求模型覆盖；实际后端模型 ID 不可见。
- 范围：同一五题、仅题面现场、文本方案，不访问真实插件工程，不浏览，不运行客户端、项目测试或 Native 模拟。本报告只写自身文件。
- 隔离：未读 baseline、其他评测报告、其他已安装 ArcheRage 技能正文。宿主上下文已有相关技能名称/描述及通用工作规则，无法排除其影响。无法自行核验 baseline 实际工具加载是否完全相同；本次不宣称严格受控因果对比。
- 实际读取：evals/README.md；behavior-cases.json 先用 PowerShell ConvertFrom-Json + Select-Object id,prompt 投影（首次误取 .cases 无输出，第二次按根数组投影成功，未展示或读取 expectations 内容）；新技能 SKILL.md；references/runtime-and-api.md、persistence-and-diagnostics.md、ui-and-lifecycle.md、replicated-suite.md、feedback.md。
- 宿主/方法混杂：另外读取 using-superpowers/SKILL.md（其 SUBAGENT-STOP 说明子代理忽略此技能）、writing-skills/SKILL.md，以及后者要求的 test-driven-development/SKILL.md，路径均在 C:/Users/23118/.codex/plugins/cache/openai-curated-remote/superpowers/6.3.0/skills/ 下。它们可能影响验证措辞和证据纪律，不能把这部分归因于新技能。
- 实际工具：functions.exec 调用 tools.exec_command，PowerShell Get-Content、ConvertFrom-Json、Select-Object、ConvertTo-Json；独立读取采用 Promise.allSettled 汇总。下方原始答案通过一次 Set-Content 写入冻结；之后才揭示 expectations，自评仅追加，不回改答案。
- 方法待办：已完成题目盲读、技能加载、作答；下一步冻结校验、揭示验收点、逐项自评、只核验报告本身。技能编写、基线运行、部署和真实项目验证均不属于本子任务。

<!-- FROZEN-ANSWERS-BEGIN -->

## AR-01 原始答复

本轮目标是让 Buff 颜色设置经现有刷新链反映到当前页面。题面入口为 v3_rebuild，toc.g 加载 presentation/v3 与 ui/framework，旧 Professional/globals/foundation audit 指引与现场不符，不应据此恢复旧框架。根指南称 tools 全删也与所给脚本存在事实冲突，但脚本存在不等于适合本故障。

后续获得工程访问时，先确认编辑目录就是客户端加载目录及实际加载版本，再沿点击回调→Commands/配置值→共享状态→失效通知/投影→当前可见 UI 实例查首个断点。比较一条能正常刷新的设置路径；若值已变但画面未变，核对订阅、generation 与实例身份，用有限且可关闭的日志取证。仅修确认的断点，不新增第二份 UI 状态。先读 rs_status_refactor_test_runner.py 的用途、参数和版本，覆盖相关链路才选用，工具无需加入 toc.g。之后重测点击改色、重开、重载及保存回读。本题未读源码或运行检查，只能提出此定位方案；无需重写插件或新增一批文档。

## AR-02 原始答复

目前能确认的是 2026-08-28 本地快照标记 GetUnitsInSight disabled、UNIT_ENTERED_SIGHT removed；今日公告访问结果缺失，不能断言最新能力已恢复。社区调用是历史/异环境线索，AAClassic 的 ADDON_API 也不能证明适用于 RU。应保持现有能力门禁，暂停接入这些调用；pcall 只捕获 Lua 抛错，既不能解除禁用，也不能证明逻辑成功。nil 要保留为未知、不可用或失败，不能伪装成成功的空列表。

调查先补实际客户端/插件加载版本、扫描入口的能力判定、错误和返回形状、玩家期望的单位范围，区分未执行、失败、成功但为空。无网无客户端时明确卡在当前能力核实；后续取得重新开放的官方证据，才在既有 Native 边界做低频、有预算、可停止的契约验证。优先复用确认可用的事件、缓存和按需刷新，不能每帧重试已禁用接口。确有合法轮询需求再定义频率、时效和停止条件；UI 标示未知及实际覆盖范围。测试正常、空结果、不可用、限制状态和重载；本轮未执行这些验证。

## AR-03 原始答复

不能先把 S 改成 A 或关闭保护，这会抹掉声明不一致的证据，也可能把坏数据合法化。pcall=true 仅表示未抛错；E==A 只能证明按当前计算得到的业务回读值符合预期，不能证明载荷声明 S 正确。当前 canonical 分支应同时检查 S 与 E 的绑定及 A==E；旧版 encoded-payload 指纹不能直接混用同一算法。

先保留原档，通过已核对的只读入口取故障 Store、自身版本及相关字段，禁止采集时迁移、补默认或保存。逐段比较业务值→编码载荷→Native 保存/回读→解码校验→应用→重登值，区分 schema、codec、transport 与 integrity。题面当前 transportVersion=3，说明写 v2 已解决不足以覆盖旧文件。x=0、透明度=0 在 Lua 中为真，不能直接归咎于 or；检查零值转换、格式分支和浮点首次变化边界，六位小数或 binary32 仅作为待验证假设，不统一舍入。最小修复是补声明校验和已定位的转换缺陷；旧档恢复另需受限候选及唯一匹配证据。回归零/false/空值、新旧格式、错误声明、错误回读、失败保护和保存回读重登。本轮无原档与客户端，不能宣称恢复成功。

## AR-04 原始答复

先保证关闭与重载的所有权清理，再沿实际几何修菜单定位。盘点现有框架的事件、订阅和任务 owner/generation；连续重载前释放旧运行时资源，初始化不能累计监听，旧 generation 的迟到回调应失效。通过活动订阅数和单次点击执行数确认重复回调来源，不能用节流掩盖它。窗口隐藏、功能停用与统计服务需求分开，停用只释放自身需求，共享统计仍由其他消费者决定。

菜单漂移先标注触发控件几何、鼠标/Native 坐标、父级变换及输出空间，确定框架已做哪些缩放，只在真实边界转换一次，并锚定当前触发实例，不能统一乘 uiScale。输入沿已有路由，核对命中层、覆盖控件和处理器，不凭名称绑定 OnKeyDown。关闭仅释放本窗口树确实拥有的编辑焦点，不能全局 ClearFocus；无所有权证据先加有限诊断。验证多档缩放、屏幕边缘、移动/重开、连续两次以上重载后点击仍一次、隐藏时其他统计消费者正常、聊天输入不中断。本题未见源码，清理和坐标接口须现场核实，以上尚未运行。

## AR-05 原始答复

报告有首尾只能说明具备检查传输完整性的边界；这里没有正文和校验结果，不能宣布校验通过。raw=0/2 且 rawStatus=copy_budget 表示两份目标原始内容都没带出；numeric=2/2 是数值摘要，不能替代 Store。旧 PART=1/10 仅为旧协议一片，不能与新报告混拼或假定其余九片内容。

两个错误仍存在是尚未解决的反馈，与上轮修回读验证漏洞并不矛盾：验证器修正、坏档恢复、界面恢复是不同结果，且还需确认补丁实际加载。最少先补同次报告的加载版本、两错误对应 Store/阶段及 raw 缺失范围；若正文已交给接收端，应直接读取其中这些字段，别让用户重发整份摘要。再按当前只读诊断协议定向导出一个最能区分假设的故障 Store 完整载荷；确认导出不迁移、补默认、应用或保存。预算不足用协议支持的定向方式，不盲要旧十段或整工程。另一个 Store 随下一步证据需要补取。

更新技能时把“报告边界完整不等于 raw 完整”“验证修复不等于历史数据恢复”归入现有诊断/反馈主题并加反例用例；Store 名称及当轮进度留项目知识。原因未证实就记假设和待验证，不写成通用恢复法，也不把本题方案记为客户端验证。

<!-- FROZEN-ANSWERS-END -->

## 揭示后逐项自评（回答者自查，非独立审阅）

冻结文件 SHA256：`EE51125051719A9900DE314D146059EA4B78A12DEAA209AD7E901AFA74F56FA4`。取得该哈希后，才用 ConvertFrom-Json + Select-Object id,expectations 读取验收点。下表仅判断冻结原文已有的具体内容，不以新技能正文代答。partial 表示主方向已有，但复合验收点中仍有遗漏；未回写补齐原答案。

| 项目 | 结果 | 原文依据与缺口 |
|---|---|---|
| AR-01.1 | pass | 指出 v3_rebuild、TOC 当前目录与旧 Professional/globals/audit 不符，并逐一指出 tools 文档冲突；明确“不应据此恢复旧框架”。 |
| AR-01.2 | pass | 首句定义颜色反映到当前页面的目标，列出“点击回调→Commands/配置值→共享状态→失效通知/投影→当前可见 UI 实例”并查首断点。 |
| AR-01.3 | partial | “仅修确认的断点，不新增第二份 UI 状态”限制了改动，但未明确在首断点确认后搜索同源调用模式及其他同因落点。 |
| AR-01.4 | pass | 要先读现存脚本用途、参数、版本，覆盖相关链才用，工具无需加入 toc.g；说明未读源码或运行检查。 |
| AR-02.1 | partial | 分开讨论社区历史/异环境线索与 AAClassic ADDON_API，不能证明 RU 可用；未显式区分 NA，也未展开签名核对。 |
| AR-02.2 | pass | 保留 disabled/removed 门禁，明确最新公告无结果；必须有重新开放的官方证据才进行合法验证，不高频探测禁用接口。 |
| AR-02.3 | partial | 区分官方恢复证据、逻辑成功、nil 与真空结果，并要求返回形状及单位范围；未显式处理导出存在和 false 返回的独立含义。 |
| AR-02.4 | pass | 使用现有 Native 边界、事件缓存按需刷新和有预算可停止验证，显示未知/覆盖范围；拒绝每帧重试，没有许诺全单位覆盖。 |
| AR-03.1 | partial | 明确当前 transport 3 与旧文件版本不同，按阶段找零值/浮点变化并纳入 false/空值；未具体列空串、稀疏表或键类型等表形检查。 |
| AR-03.2 | partial | 区分未抛错、E/A、S 声明、历史指纹及重登验证，提出 canonical 的 S-E 绑定与 A==E；未明确写出检查 Native SaveData 的逻辑返回结果。 |
| AR-03.3 | pass | 拒绝重盖 S、关闭保护，要求保留原档及禁止采集时迁移/补默认/保存，恢复须受限候选和唯一匹配证据。 |
| AR-03.4 | pass | 修复限于声明校验与已定位转换缺陷，列正常/失败回归；六位小数/binary32 被称为待验证假设，无原档不能宣称恢复。 |
| AR-04.1 | partial | 标注触发控件、鼠标/Native、父级和输出空间，锚当前实例并只转换一次；未分别讨论世界投影和自由窗口的语义差别。 |
| AR-04.2 | partial | 检查 owner/generation、任务订阅资源、旧回调失效和订阅/执行数，拒绝节流；未明确列出旧控件实例释放的核对。 |
| AR-04.3 | pass | 关闭限于本窗口树拥有的焦点，拒绝全局 ClearFocus；不能凭名称绑定 OnKeyDown，沿已有路由核查，验证聊天不中断。 |
| AR-04.4 | pass | 将窗口隐藏、功能停用、共享服务需求分开，保留其他消费者统计；给出多档缩放、重开、重复重载等回归并说明尚未运行。 |
| AR-05.1 | pass | 首尾只是可检查边界，无正文校验不能宣布通过；copy_budget 表示两份 raw 缺失，numeric 不替代 Store，旧 PART 不可拼补。 |
| AR-05.2 | pass | 明确两个错误未解决与验证漏洞修复不矛盾，区分验证器/坏档/UI；仅说需要确认实际加载，没有把失败断言为未加载。 |
| AR-05.3 | pass | 先核同次加载版本、Store/阶段及范围，已有报告应直接读，定向要一份最能区分假设的 Store，不要重发摘要或整工程。 |
| AR-05.4 | partial | 明确未证实原因留假设/待验证、Store 进度归项目知识，未把方案冒充实测；没有完整写出预期/实际/版本/证据/原因/修复/实测结果的记录结构。 |

## 结论与验证边界

自评共 20 项：12 pass、8 partial、0 fail。主要遗漏是同源位置搜索、证据类型和返回状态的完整区分、存档表形/逻辑返回、UI 各类坐标及控件清理、完整反馈记录字段。此处只指出缺口，没有据此修订冻结答案或技能。

这是一个加载后条件下的五题文本冒烟评测；不是用户验收，也不是独立评分。没有读取基线，因此不报告改善幅度；真实改善未证明。实际工具仅用于阅读评测材料、写本报告、计算冻结哈希及核验追加前后原始字节不变、评分条目数；未运行插件代码、语法检查、模拟、项目测试或游戏客户端。没有凭借本次评测宣称 RU 实际行为已验证。
