# ArcheRage 独立评估（2026-09-12）

评估者为独立子代理；后端模型 ID 不可见。使用 writing-skills 与 skill-creator 的行为验证原则。仅写此报告，不修改技能或游戏项目。

## 触发判断冻结（揭示预期前）

在本节写入前，仅阅读新技能的 YAML name/description，以及触发数据的 id/prompt/context 投影；未阅读 expected/reason、技能正文或行为预期。文件首次定位更正为 skills/skillforge-archerage-addon/evals/trigger-cases.json；属性名 inspected，但未输出其预期值。

| ID | 冻结判断 | 依据 |
|---|---|---|
| AR-T01 | 触发 | 明确 RU 客户端 Lua addon，重复刷新属于调试。 |
| AR-T02 | 触发 | 上下文明确 RU 的 Replicated Suite 现有插件开发。 |
| AR-T03 | 触发 | RU 项目诊断反馈解析需要该域知识；技能完善另配技能编写流程。 |
| AR-T04 | 不触发 | UE5 Lua 桥有明确其他运行平台，排除。 |
| AR-T05 | 不触发 | AAEmu C# 服务端寻路不属客户端 addon。 |
| AR-T06 | 不触发 | 独立 AAClassic 且没有 RU 迁移目标，排除。 |
| AR-T07 | 先确认 | 只知上古世纪插件；服务器分支未明确，不能默认 RU。 |
| AR-T08 | 先确认 | Lua 来源与目标不明，需确认目标运行环境。 |
| AR-T09 | 条件触发 | 当前上下文本身为条件：确认 RU 则触发并组合 UI；其他项目不触发。 |

此节保持原判断，后续比较不回改。

## 揭示后的触发比较

AR-T01–03 对应 trigger，AR-T04–06 对应 no_trigger，AR-T07–09 对应 conditional；冻结判断 9/9 一致。这里证明描述能支持这九个给定上下文的人工选择，不证明宿主会自动发现、加载或组合该技能。

## Baseline 独立逐项评估

材料是 archerage-baseline.md 的冻结回答区；该文件也包含作者自查，读取时可见，所以本审阅不是对自查结果盲审。以下重新按行为用例原始 expectations 判断，不以作者判定或关键词作为证据。pass 为完整支持要点，partial 为行为方向正确但存在实质缺项，fail 为缺乏支持或相反。未把本轮技能正文或实际源码复核补入 baseline 的回答证据。

| 用例/项 | 判定 | 冻结回答的具体行为依据及缺口 |
|---|---|---|
| AR-01/1 | pass | 回答选择 v3_rebuild、TOC 实际链和文件存在情况，明确拒绝恢复旧 globals 等，并指出根指南 tools 描述过时。 |
| AR-01/2 | partial | 追控件回调、配置键、失效通知和重绘，能区分保存/通知/消费失败；“改色即显”只是拟议验证，未先核定颜色应作用于哪个当前实例。 |
| AR-01/3 | partial | “只沿现有链做最小修复”限制了范围，但没有搜索同源模式后逐落点确认相同前提的操作。 |
| AR-01/4 | pass | 先读现有 runner 用途及架构适用性再选择，明确本轮无工程访问，未建议把工具当生产加载依赖。 |
| AR-02/1 | pass | 明确 AAClassic 与社区实现只能作线索，要求核对目标服务器和版本再判断 RU；不要求逐字列出 NA 才算区分宿主。 |
| AR-02/2 | partial | 不把旧快照或没有访问结果冒充最新事实；但“核实当前官方说明并做隔离能力探测”没明确恢复公告是后续探测禁用函数的前置门禁。 |
| AR-02/3 | partial | 拆开 pcall 无异常、nil、错误、未知及真空结果，尚未分别判断导出可见、false 逻辑返回及覆盖的单位范围。 |
| AR-02/4 | pass | 接已有缓存/受控刷新，选已验证事件或有限轮询，并提供降级；明确否定每帧扫描，没有承诺替代 API 全覆盖。 |
| AR-03/1 | partial | 比较 v3 写出与 v2 旧档，检查 0、浮点和排序，纠正 Lua 0 为真的事实；false、空串、表形尚未纳入差异检查。 |
| AR-03/2 | partial | 知道 pcall=true 与 E==A 不足以验证 S；未列出逻辑成功、重载以及当前 canonical/历史 encoded 算法分支的完整判据。 |
| AR-03/3 | pass | 开头拒绝改写 S/关保护并说明会抹掉证据，先保全原文件再取差异；没有先默认化/迁移/保存原档的方案。 |
| AR-03/4 | partial | 修复候选由指纹输入与迁移时序差异引出，没有把数值假设写成事实；验证只有零值/浮点/往返，失败路径与保护保持未覆盖。 |
| AR-04/1 | pass | 先标明坐标空间及已有缩放再转换一次，明确不能给所有坐标乘 uiScale；没有把不同场景强行套为统一乘法。 |
| AR-04/2 | partial | 检查旧订阅/闭包/菜单实例释放，要求重复初始化不累积、每次只执行一次；缺少旧代任务释放和活动订阅计数检查。 |
| AR-04/3 | pass | 采用已有输入路由，只处理自己拥有的焦点，拒绝通用 OnKeyDown + 全局 ClearFocus，并要求验证聊天焦点。 |
| AR-04/4 | pass | 展示订阅可释放但统计需看其他消费者和服务生命周期，且列出显示切换、缩放、连续重载回归，均为未执行方案。 |
| AR-05/1 | pass | 将首尾边界、raw 缺失、numeric 摘要和 PART 缺片分别解释，没有从摘要或缺片重建原文。 |
| AR-05/2 | pass | 把“验证漏洞代码已改”和“两处 Store 故障待复现”拆开，不据错误仍在断言补丁未加载或无效。 |
| AR-05/3 | partial | 请求的错误/Store/载荷确实相关，但一次要求两处完整材料，未先核对同次版本和范围再选择最能区分假设的一份；分批复制也未核对当前协议支持。 |
| AR-05/4 | partial | 提出补充完整性、缺片与结论边界，但没有形成预期、实际、版本、证据、原因、修复、实测结果的可维护反馈记录。 |

独立结果：20 项，pass 10、partial 10、fail 0。与作者自查计数相同不是额外重复实验；原回答已经正确的行为不能归因于新技能。若需一个汇总比例，完整满足为 10/20；本报告不把 partial 任意换算成半分。

## 内容、来源与维护审阅

- 技术范围：客户端 RU 的加载/API、存档、UI/输入和生命周期均有实质操作判断；明确排除 AAClassic 与 AAEmu 的宿主误用。不可用能力在更新证据前保持门禁，数据未知、失败和真实空值分开。未发现必须阻止本版采用的技术错误。
- Replicated Suite 边界：项目 reference 仅在当前工程确为 RS 时读；存档 S/E/A 小节也限定当前代码仍用该模型。通用正文的 Commands/服务/投影链补有“以项目真实分层为准”，没有要求普通插件搭建整套 RS 架构。旧页、globals、固定页签、全量审计、固定个人目录和工具加载强制规则均未被移植为通则。
- 来源可追溯性：社区参考固定到提交，区分 fork 同源与独立证据；官方来源有具体页及更新日期，明确 9 月 2 日读取缺口。本独立审阅没有再次远程抓取这些页面，因此只确认引用组织、边界和本地可复核性，不冒充第二次官方核验。
- 许可证：来源声明没有找到明确授权，未将公开访问等同复用许可，正文为方法归纳，包中没有社区 Lua 源码。未见复制实现或长段转载的迹象；这属于内容审阅，不是重新扫描所有远程仓库或法律许可鉴定。
- 维护体量：入口 44 行/4196 字节，6 个主题参考共 237 行/21738 字节；另有 9 个触发用例及 5 个行为用例。入口按需路由并明确普通改色不读全套，主题间少量交叉提示有助发现关键边界，未见无关资源或仅为目录整齐的占位文件。日期快照与版本特例集中在 sources/replicated-suite，后续应保持原位更正及明确时点，避免把本轮架构冻结为永久规则。
- 游戏验证表述：完成标准区分源码、本地模拟和客户端；来源将用户历史反馈、项目报告模拟和本轮静态核对分开；UI 重载双击题注明为合成案例。没有以本地模拟、静态规范或这组答案证明真实插件已修好。

### 独立源码指纹与关键契约复核

6 个来源文件重新只读计算 SHA-256；toc.g、replicatedsuite.lua、rs_api.lua、rs_api_capabilities.lua 四项仍与来源表一致。另两项在独立复核时已变化：

| 文件（相对 Addon 根） | 独立复核 SHA-256 |
|---|---|
| replicatedsuite/core/rs_persistence.lua | 5eb5da62cd955b9d1f7e842c4d3968b01bc21d2fb5d734a9bc41a612adfc9d8b |
| replicatedsuite/core/rs_self_check_report.lua | 67f9c133651c4955407e98ea62873e8c2e6b388406454bbcf56327d48e66c83b |

主代理确认其他任务正在修改游戏工程，故保留 sources.md 的历史采样指纹，不用后来现场覆盖原记录。原 HEAD + 脏树说明及哈希能标识读过的状态，不能单凭 Git HEAD 重建那一刻全文。

对变化文件只读检查相关实现：当前 rs_persistence.lua 的 TransportContractVersion 仍为 3，IntegrityContractVersion 为 4（两者不能混为同一版本）。当前 Verify 的 canonical 分支先比较声明值与 expected，再比较 actual，允许的表示恢复依然通过既有校验函数；历史 encoded 分支另行校验，符合技能的条件描述。当前 report 仍在 raw 包超预算时清空 packet 并记 copy_budget，coverage 的 raw 与 numeric 分列，符合缺原文不等于缺报告外壳的判断。现在代码新增 next=udf_snapshot 导向，技能没有写死旧分片协议或断言该新入口可直接执行，因此未发现正文结论因这一变化失效；实际使用必须重新读本轮入口和隐私/操作边界。未调用保存/恢复/客户端脚本，未读 Git remote/config 或凭证。

## 结构结果及验证限制

主代理报告实际执行 `python -B scripts/validate_skills.py --self-contained`：Validated 5 skill(s): 0 error(s); 5 standalone copies validated。此为主代理提供的结构验证证据，本审阅未重复运行。独立阅读确认新包 reference 链完整，包内没有个人绝对路径或宿主专属工具调用；结构通过只证明该检查器覆盖的格式/可移植性约束。

触发 9/9 是描述匹配检查。Baseline/forward 是模型对合成题目的方案回答，应按具体行为判断，不等于在游戏中执行方案。单次同设置对照存在宿主背景混杂、随机差异和用例有限性；具体后端模型 ID 不可见。不能据此保证其他模型、其他 Agent 或实际客户端任务通过。

## Forward 独立逐项评估（已完成）

收到主代理完成通知后，读取 archerage-forward.md 至 FROZEN-ANSWERS-END 的冻结区，未读取其后逐项自评。主代理通知中已透露作者总计 12 pass/8 partial，因此总计层面不是盲审；本节按与 baseline 相同的行为标准独立评分，不采纳该总计作为目标，也不要求逐字列举全部示例名才能通过。

| 用例/项 | 判定 | 冻结回答的具体行为依据及缺口 |
|---|---|---|
| AR-01/1 | pass | 依据 v3_rebuild、TOC 和实际存在脚本定位，拒绝恢复旧框架并指出 tools 文档冲突。保持正确。 |
| AR-01/2 | pass | 开头定义颜色经现有刷新链反映到当前页面；后文完整追点击/命令/共享状态/失效通知/当前实例，并用正常设置路径比较首次断点及 generation/实例身份。比 baseline 具体。 |
| AR-01/3 | partial | 限定仅改已确认断点且不新建 UI 状态，但仍没有搜索同源问题、逐落点确认相同原因。 |
| AR-01/4 | pass | 明确现有 runner 需用途/参数/版本覆盖后选用，工具不加 TOC，未运行检查。保持正确且加载边界更显式。 |
| AR-02/1 | pass | 社区来源仅作历史或异环境线索，Classic 不能证明 RU，调查实际客户端版本。与 baseline 同样以宿主区别的具体判断满足该项，无需机械列出 NA。 |
| AR-02/2 | pass | 明确保持门禁、暂停接入，只有取得重新开放的官方证据才做既有边界契约验证；当前没有访问结果不冒充最新事实。修补 baseline 的探测前置条件缺口。 |
| AR-02/3 | partial | 区分 pcall、逻辑成功、nil/错误/空列表，要求返回形状和玩家期望单位范围；仍未把导出可见与可调用、false 与空结果分别作为独立检查。 |
| AR-02/4 | pass | 先复用可用事件缓存，轮询限频/预算/停止条件且 UI 披露覆盖，拒绝每帧重试禁用 API。保持正确。 |
| AR-03/1 | partial | 对 schema/codec/transport/integrity 和旧档分开，零值/浮点有调查，false/空值在回归里被覆盖；未在调查中展开稀疏表、键类型或表形，故未完整满足。 |
| AR-03/2 | partial | S/E 绑定和 A==E、历史 encoded 算法隔离、保存到重登链均已明确；尚未单独核对 SaveData 的逻辑结果，pcall 成功后应检查什么业务返回没有落地。 |
| AR-03/3 | pass | 禁止改 S/关保护，取证入口不得迁移补默认保存；旧档恢复限候选及唯一匹配。保持正确且比 baseline 更明确。 |
| AR-03/4 | pass | 修复限定声明校验和已定位转换缺陷，数值表示只作假设，不统一舍入；回归新旧、正常空值、错误声明/回读、失败保护及重登，且不宣称恢复。补足 baseline 的失败回归。 |
| AR-04/1 | pass | 输入/输出几何、Native/父级变换、已有缩放和当前锚定实例均需先识别才转换。虽未逐字列出世界投影/自由窗，但该方法明确拒绝同乘法，可直接区分不同空间；与 baseline 采用相同标准。 |
| AR-04/2 | pass | 盘点事件/订阅/任务的 owner/generation，旧代释放和迟到回调失效、重载不累积，用活动订阅数与单次执行数确认原因。补足 baseline 的任务与代际缺口。 |
| AR-04/3 | pass | 先核对路由及命中层，不凭名称绑定事件；只处理当前窗口树拥有焦点，缺证据先诊断且回归聊天输入。保持正确。 |
| AR-04/4 | pass | 隐藏/停用/共享需求三者区分，其他消费者保留统计；多档缩放、重开、重复重载和聊天回归均标为尚未运行。保持正确。 |
| AR-05/1 | pass | 报告有首尾仅能检查边界，并未冒称校验通过；raw 缺失、numeric 摘要、旧 PART 片段彼此不混用。保持正确且外壳校验判断更严谨。 |
| AR-05/2 | pass | 当前错误与验证漏洞修复不矛盾，修正/恢复/UI 恢复分开；补丁加载需核实而非直接断言缺失。保持正确。 |
| AR-05/3 | pass | 先核同次版本、Store/阶段与 raw 范围，已给材料直接读；再取一个能区分假设的定向只读载荷，其余 Store 按证据需求后取。补足 baseline 的一次请求过多与协议核对缺口。 |
| AR-05/4 | partial | 更新现有主题、加反例，候选原因不升格为通用恢复法，未实测不冒充；但未形成包含预期/实际/版本/证据/原因/修复/实测的反馈记录。 |

独立结果：20 项，pass 15、partial 5、fail 0。作者自评总计与此不同，属于评分尺度差异，不是第二份行为输出。本独立审阅对同一两组答案保持相同标准：依据能执行的判断步骤评分，而不是要求完全复述验收点例词。

### 本次对照支持什么

- 10 个 baseline 已完整支持的验收点在 forward 保持正确。
- 5 项在此次回答中由 partial 到 pass：AR-01/2 的可见目标/当前实例链、AR-02/2 的官方重新开放前置门禁、AR-03/4 的失败保护回归、AR-04/2 的旧代任务/订阅清理、AR-05/3 的同次范围核对与单份最小证据。
- 仍有 5 项缺口：同源问题搜索；API 导出/false 状态的独立判定；表形与键类型调查；SaveData 逻辑返回核查；完整反馈记录结构。
- 上述变化是这一次文本输出更具体的证据，不证明技能对真实游戏修复成功率的因果改善。Forward 比 baseline 另外加载了 writing-skills 和 test-driven-development，除不可见后端模型与宿主背景外，这也是无法排除的混杂。

### 输入版本与最终边界

主代理提供且声明 forward 起未修改的包指纹：`5f284d5feae4749bda18f879d1e3e8de6450660a9aea4fd1d61bea75cdb2b8ee`。算法是包内全部 9 文件各作 `相对路径（/）:小写SHA256`，按路径排序，以 LF 连接并加末尾 LF，UTF-8 无 BOM 编码后再 SHA-256；本段记录提供的指纹，不冒充独立重算。

本审阅的结论是：结构检查有明确通过记录，九个描述选择一致；五题单次文本冒烟有若干更具体的行为，但五项仍未完整覆盖。没有实际游戏测试、用户原始坏档恢复、源码修复执行或用户验收。本次只新增/追加这一份 assessment 报告；所有真实项目访问为只读。
