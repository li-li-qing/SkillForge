# 动画经验第二轮补齐

本轮根据用户要求继续完善 SkillForge，未操作 LGame 源码、蓝图、动画资产或已安装技能。

## 具体增加与纠正

1. 新增 animation-delivery-workflow：素材覆盖表 → 标准骨架与周期 → 当前 Chooser 真值表 → 实际播放 → 分层/模拟/附件 → 完整无伤害预览 → 人工逐击正式配置 → 分层验收。
2. 补准单一源 Socket：共用父手骨局部校准，套到可见角色的重定向手骨，Authority Trace 仍跟服务器源；修正先前“随源骨架”的含混表达。不是源世界坐标直拷，也不强制新建 IK 系统。
3. 补覆盖曲线实作：普通浮点与 Metadata 区别、缺失/非有限/部分覆盖、BasePose 单次求值、Game Thread 同步与 PreUpdate 快照、HasPreUpdate/当前生成类登记、混合/切风格恢复、局部与网格空间旋转。
4. 补 Chooser 行误选：不默认 row0 是循环行，不按 raw state/bit 猜语义，检查真实上下文命中、遮蔽及外来绑定。
5. 补生成器门禁：共享严格规格、修改前参数互斥、求值前错误计数、官方公开 API、持久化负测、恢复与索引证据。
6. 补用户人工交接：真实入口、按键、资产字段、预览动作和刷新要求；不让用户用握持偏移掩盖未修好的基础姿态。

新增三个行为题 LGF-13—15。原十二题保留。按需入口已接入 LGF SKILL.md 及库 README，不复制另一份项目总状态表。

## 证据与验证

当前源码/合同选段的路径与 SHA 在 evidence.json；本轮关注 Source Socket 解析、WeaponOverlay、Coverage 辅助与测试。DirectionReview 是历史报告，不当作本轮运行。未执行 UE 构建、资产编辑、PIE、视觉、联网或正式伤害测试。

库内六个包的结构/链接/独立复制检查通过；LGF 官方 quick_validate 通过。仅文档和评测数据修改，没有再次把上一轮 75 项工具回归抄成本轮重跑。结果见 packages.txt 与 official-lgf.txt。

新上下文文字样本分为 baseline.md 与 updated.md。基线已知道不能盲目复制世界位置，但没有给出本项目手骨局部组合，还提出源 Mesh 组件空间映射的条件假设；技能现在明确本案例的实际实现及适用条件。基线对 PreUpdate/零伤害的通用边界已基本正确，不虚构其全部失败。具体回答比对不替代引擎执行，也不形成跨模型正确率保证。

## 备份与清理状态

LGF 改前内容及 README 保存在 `D:/Project/Skiils/MigrationBackups/Animation_20260914_104924`，见 backup.json。初次清点误将正在输出的 SHA256.csv 自身纳入而读取失败，随后只枚举已复制的技能文件，重新生成有效清单；不将第一次错误称为成功。

用户再次明确授权删除旧目录后，本轮重新核验 91 文件与完整备份 SHA 全相同，并检查无重解析点。直接 Remove-Item 仍被自动审批以 `blocked by policy` 拒绝，没有详细原因，实际没有删除；见 deletion-retry.json。未切换工具绕过。原目录与完整恢复备份均保留，故删除任务仍未完成。

## 补充行为题判读

独立上下文的 updated.md 已完成三题回答，主作者逐项判读：

| 场景 | 文字验证结果 | 证据边界 |
| --- | --- | --- |
| LGF-13 单一源 Socket | 覆盖父手骨局部组合、可见手骨、服务器 Trace 分离及空间校验 | 未运行实际附件 |
| LGF-14 Coverage/PreUpdate | 覆盖当前生成类登记、实际调用、线程快照、单次求值与缺失曲线处理 | 未验证引擎执行 |
| LGF-15 新武器全过程 | 覆盖真实 Chooser 行、素材周期、实际播放、零伤害预览与人工窗口审核 | 未制作或验收游戏资产 |

三个样本均覆盖本轮预期边界；这是技能文字使用检查，不是独立盲评分、统计效果保证或游戏验收。
